{
  "name": "RadioButton",
  "status": "new",
  "figma": {
    "fileKey": "YzxnyL6a69WCOw9U8WJqBo",
    "nodeId": "250:1224"
  },
  "import": "import { RadioButton } from '@yokesh-2one/design-library'",
  "description": "Single-select control for one option from a mutually exclusive group. Not for multi-select (use Checkbox) or on/off (use Switch).",
  "props": {
    "label": "string?",
    "name": "string?",
    "isInvalid": [
      "true",
      "false"
    ],
    "disabled": [
      "true",
      "false"
    ]
  },
  "rules": [
    "Only one selected per group — the group enforces this (share a `name`).",
    "Don't combine isInvalid with disabled.",
    "Error text lives at the group level, not per radio."
  ]
}
