{
  "name": "Dropdown",
  "status": "new",
  "figma": {
    "fileKey": "YzxnyL6a69WCOw9U8WJqBo",
    "nodeId": "230:631"
  },
  "import": "import { Dropdown } from '@yokesh-2one/design-library'",
  "description": "Single-select form field that opens a panel of options. Trailing chevron always present (down when closed, up when open). Not for typeable/filterable lists or multi-select.",
  "props": {
    "label": "string?",
    "value": "string?",
    "placeholder": "string?",
    "leadingIcon": "ReactNode?",
    "helperText": "string?",
    "isInvalid": [
      "true",
      "false"
    ],
    "disabled": [
      "true",
      "false"
    ],
    "open": [
      "true",
      "false"
    ]
  },
  "rules": [
    "Don't combine isInvalid with disabled.",
    "Don't combine active(open) with disabled.",
    "Single-select only — use a Multiselect field for many.",
    "This is the trigger field; the options panel is a separate component."
  ]
}
