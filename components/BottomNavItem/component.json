{
  "name": "BottomNavItem",
  "status": "new",
  "figma": {
    "fileKey": "YzxnyL6a69WCOw9U8WJqBo",
    "nodeId": "265:1453"
  },
  "import": "import { BottomNavItem } from '@yokesh-2one/design-library'",
  "description": "A single destination item within a bottom navigation — icon + label — repeated in a row to form the tab bar. State: default / pressed / selected.",
  "props": {
    "icon": "ReactNode",
    "label": "string",
    "selected": [
      "true",
      "false"
    ],
    "onClick": "() => void ?"
  },
  "rules": [
    "This is one tab item, not the whole bar — compose several in a row.",
    "Only one item selected at a time (the bar enforces this).",
    "Icon is swappable per instance."
  ]
}
